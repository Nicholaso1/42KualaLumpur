#include <stddef.h>

void    *ft_memset(void *dst, int src, size_t len)
{
    size_t  i;
    unsigned char *ptr;

    i = 0;
    ptr = (unsigned char *)array;
    while (i < len)
    {
        ptr[i] = (unsigned char)c;
        i++;
    }
    // ptr[i++] = (unsigned char c)
    return array;
}

int main(void)
{

}
